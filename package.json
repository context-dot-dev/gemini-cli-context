{
  "name": "gemini-cli-context",
  "version": "1.0.0",
  "private": true,
  "description": "Official Context.dev extension for Gemini CLI",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:live": "node scripts/validate-live.mjs",
    "validate": "gemini extensions validate ."
  },
  "devDependencies": {
    "@google/gemini-cli": "0.58.0"
  },
  "engines": {
    "node": ">=20"
  }
}
